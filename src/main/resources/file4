
Основные команды Git для терминала:

git commit -a -m "Сообщение"   # То же самое, что последовательное выполнение git add . и git commit -m "Сообщение коммита
git reset
git add .
git commit --amend -m "Сообщение коммита" # Дополняет (перезаписывает) последний коммит, добавляя в него "свежие" изменения.
                                            Также, меняет сообщение последнего коммита. Новый коммит не создается!

git reset head^     # Возвращает проект к указанному коммиту, при этом переводит
                      все коммиты после указанного в неотслеживаемую (unstaged)
                      зону. head^ - предыдущий head
git remote -v
git remote remove PP01
git remote add PP01 git@github.com:darsiya77/Darsiya77.git